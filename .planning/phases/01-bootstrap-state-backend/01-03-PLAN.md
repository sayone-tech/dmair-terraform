---
phase: 01-bootstrap-state-backend
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - envs/strapi/backend.tf
autonomous: false
requirements:
  - BOOTSTRAP-02

must_haves:
  truths:
    - "envs/strapi/backend.tf declares dynamodb_table = \"dmair-terraform-locks\" inside the backend \"s3\" {} block"
    - "envs/strapi is re-initialized via terraform init -reconfigure (NOT -migrate-state) without prompting for state migration"
    - "terraform plan in envs/strapi reports 'No changes. Your infrastructure matches the configuration.' — proves zero-change-plan invariant under live-infra-is-sacred (PROJECT.md Core Value) — first of three live-stack rewires (D-11)"
    - "The rewire is a single, independently-revertable commit (D-13)"
  artifacts:
    - path: "envs/strapi/backend.tf"
      provides: "Backend block with dynamodb_table line added; identical to current shape otherwise"
      contains: 'dynamodb_table           = "dmair-terraform-locks"'
  key_links:
    - from: "envs/strapi/backend.tf dynamodb_table argument"
      to: "AWS DynamoDB table dmair-terraform-locks (created in Plan 01-02)"
      via: "string-literal contract (D-06)"
      pattern: 'dynamodb_table = "dmair-terraform-locks"'
    - from: "envs/strapi terraform plan"
      to: "ZERO-CHANGE-PLAN gate"
      via: "live-infra-is-sacred invariant (PROJECT.md)"
      pattern: "No changes. Your infrastructure matches the configuration."
---

<objective>
Rewire the Strapi CMS stack to use the new DynamoDB lock table via a one-line edit to `envs/strapi/backend.tf`, re-initialize the backend, and verify a zero-change plan. First of the three live-stack rewires (D-11 commit order: `envs/strapi` → `envs/frontend/prod` → `envs/frontend/staging`).

Purpose: Strapi is the highest-risk rewire (longest apply, most managed resources — EC2 with `prevent_destroy=true`, EIP, IAM, SG, CloudFront). Doing it FIRST under the zero-change-plan gate proves the rewire mechanism on the riskiest target before touching the lower-risk frontend stacks (D-11 rationale).

Output: One line added to `envs/strapi/backend.tf`. One independent atomic commit.

Hard invariant: ZERO resource managed by `envs/strapi` may change. `terraform plan` after `init -reconfigure` MUST report `No changes`. If it doesn't, revert this single commit and debug (D-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-state-backend/01-CONTEXT.md
@.planning/phases/01-bootstrap-state-backend/01-RESEARCH.md
@.planning/phases/01-bootstrap-state-backend/01-PATTERNS.md
@.planning/phases/01-bootstrap-state-backend/01-02-SUMMARY.md
@envs/strapi/backend.tf
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add dynamodb_table line to envs/strapi/backend.tf</name>
  <files>envs/strapi/backend.tf</files>
  <read_first>
    - envs/strapi/backend.tf (lines 1-9 — current 9-line shape; insertion target is between line 7 (`shared_credentials_files`) and line 8 (closing brace))
    - .planning/phases/01-bootstrap-state-backend/01-PATTERNS.md (§envs/strapi/backend.tf — exact edit + alignment specification; §Shared Patterns §HCL Formatting — column-30 alignment)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Pattern 3 lines 242-259 — full final shape; §Pitfall 6 — deprecation warning is expected and not a diff)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-06 — contract string "dmair-terraform-locks"; D-11 — sequencing rule, this is the FIRST rewire; D-13 — single-commit revertability)
  </read_first>
  <action>
    Insert exactly ONE line into `envs/strapi/backend.tf` inside the `backend "s3" {}` block, immediately after the `shared_credentials_files` line and before the closing brace. The new line:
    `    dynamodb_table           = "dmair-terraform-locks"`

    Spacing: 4-space leading indent (matches the surrounding block), `dynamodb_table` followed by 11 spaces to align the `=` at column 30 (matches the existing `bucket`, `key`, `region`, `profile`, `shared_credentials_files` `=` column). Value is the contract string per D-06.

    Do NOT change any other line. Do NOT remove the `# us-west-2` inline comment on the `region` line. Do NOT touch any other file in `envs/strapi/` (no `main.tf`, `providers.tf`, `variables.tf`, etc.). Implements D-11 (first rewire of three) and D-06 (contract string).

    Run `terraform fmt envs/strapi/backend.tf` after the edit to confirm formatting is canonical.
  </action>
  <verify>
    <automated>grep -q 'dynamodb_table           = "dmair-terraform-locks"' envs/strapi/backend.tf &amp;&amp; test $(wc -l &lt; envs/strapi/backend.tf) -eq 10 &amp;&amp; terraform fmt -check envs/strapi/backend.tf</automated>
  </verify>
  <done>
    - `envs/strapi/backend.tf` is 10 lines (was 9; +1 line for `dynamodb_table`)
    - Contains the exact line `    dynamodb_table           = "dmair-terraform-locks"` with `=` at column 30
    - `terraform fmt -check` reports no diff
    - No other file in `envs/strapi/` is modified
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator runs terraform init -reconfigure + plan in envs/strapi and verifies zero-change</name>
  <read_first>
    - envs/strapi/backend.tf (just-edited, +1 line)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Code Examples §Example 6 lines 629-645 — exact rewire sequence; §Pitfall 5 lines 378-382 — never accept the -migrate-state prompt; §Pitfall 6 — deprecation warning is informational)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-11 — sequencing; D-12 — no migrate-state expected; D-13 — revert this commit if non-empty plan)
  </read_first>
  <what-built>Claude added one line to `envs/strapi/backend.tf`. The operator must now re-initialize the backend with `terraform init -reconfigure` (NOT `-migrate-state`) and verify `terraform plan` reports zero changes.</what-built>
  <how-to-verify>
    From repo root, run the following commands IN ORDER:

    1. `cd envs/strapi && terraform init -reconfigure`
       - Expected: `Reusing previous version of hashicorp/aws from the dependency lock file`, then `Successfully configured the backend "s3"!`.
       - If Terraform prompts `Do you want to copy existing state to the new backend? (yes/no)` — answer NO and STOP. This is the migrate-state prompt (Pitfall 5 / D-12); a metadata-only reconfigure should not trigger it. Investigate the `.terraform/` cache before retrying.

    2. `terraform plan`
       - **CRITICAL — BOOTSTRAP-02 success gate (first of three).** Expected: `No changes. Your infrastructure matches the configuration.`
       - A Terraform 1.11+ deprecation warning on `dynamodb_table` is EXPECTED (Pitfall 6). It appears as a separate warning section; the `Plan:` / `No changes.` summary is what counts.
       - If the plan reports ANY managed-resource diff: STOP. Do NOT commit. Revert the one-line edit to `envs/strapi/backend.tf` (the commit is independently revertable per D-13), and escalate to the human with the full plan output. The most likely cause is a stale `.terraform/` cache; see Pitfall 5 recovery procedure.

    Paste the final `Plan:` / `No changes.` line from step 2 (NOT the deprecation warning text) into the resume message.
  </how-to-verify>
  <acceptance_criteria>
    - `cd envs/strapi && terraform init -reconfigure` exits 0 without prompting for `-migrate-state`
    - `cd envs/strapi && terraform plan` exits 0 with stdout containing the exact string `No changes. Your infrastructure matches the configuration.`
    - No managed resource shows a diff (`Plan: 0 to add, 0 to change, 0 to destroy.` or the equivalent "No changes" form)
    - Operator commits the one-line edit as a standalone commit titled `feat(BOOTSTRAP-02): wire strapi backend to dmair-terraform-locks DynamoDB lock table` (or equivalent — must be independently revertable per D-13)
  </acceptance_criteria>
  <resume-signal>Type "strapi-rewired" with the No-changes plan output line, or describe the failure and the revert action taken.</resume-signal>
  <files>(operator-only checkpoint — no files modified by Claude)</files>
  <action>See &lt;how-to-verify&gt; above. This is an operator-only checkpoint — Claude does NOT execute commands. The orchestrator MUST present &lt;what-built&gt; and &lt;how-to-verify&gt; to the human operator and wait for the resume signal.</action>
  <verify>
    <automated>echo 'checkpoint: human verification required — see &lt;acceptance_criteria&gt;'; true</automated>
  </verify>
  <done>See &lt;acceptance_criteria&gt; above. Human operator must confirm via resume-signal.</done>
</task>

</tasks>

<verification>
- envs/strapi/backend.tf has exactly one added line: `dynamodb_table = "dmair-terraform-locks"`.
- `cd envs/strapi && terraform plan` reports `No changes`.
- The edit is committed as a single atomic commit (revertable per D-13).
- `envs/frontend/prod` and `envs/frontend/staging` are NOT touched in this plan (they are Plans 01-04 and 01-05 respectively, per D-11 sequencing).
</verification>

<success_criteria>
- BOOTSTRAP-02 partial: 1 of 3 live-stack rewires complete (strapi) with zero-change plan verified.
- Live-infra-is-sacred invariant maintained: no resource managed by `envs/strapi` changed as a result of the lock-table wire.
- Plan 01-04 (`envs/frontend/prod` rewire) is now safe to start.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-state-backend/01-03-SUMMARY.md` documenting:
- Confirmation that `envs/strapi/backend.tf` is now 10 lines with the dynamodb_table line present
- Pasted `terraform plan` "No changes" output
- The commit SHA of the rewire commit
- Confirmation that no other file in `envs/strapi/` was touched
</output>
