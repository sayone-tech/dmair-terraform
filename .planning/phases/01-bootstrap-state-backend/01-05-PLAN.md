---
phase: 01-bootstrap-state-backend
plan: 05
type: execute
wave: 5
depends_on:
  - "01-04"
files_modified:
  - envs/frontend/staging/backend.tf
  - envs/frontend/staging/providers.tf
autonomous: false
requirements:
  - BOOTSTRAP-02

must_haves:
  truths:
    - "envs/frontend/staging/backend.tf declares use_lockfile = true inside the backend \"s3\" {} block (S3-native state locking)"
    - "envs/frontend/staging/providers.tf declares required_version = \"~> 1.15\" inside the terraform {} block — use_lockfile requires Terraform >= 1.10; workstation runs 1.15.3"
    - "envs/frontend/staging is re-initialized via terraform init -reconfigure (NOT -migrate-state) without prompting for state migration"
    - "terraform plan in envs/frontend/staging reports 'No changes. Your infrastructure matches the configuration.' — third and final of three live-stack rewires (D-11)"
    - "BOOTSTRAP-02 fully satisfied: all three live backends now use S3-native state locking (use_lockfile = true) AND have required_version = \"~> 1.15\" pinned in providers.tf"
  artifacts:
    - path: "envs/frontend/staging/backend.tf"
      provides: "Backend block with use_lockfile line added; identical to current shape otherwise"
      contains: 'use_lockfile             = true'
    - path: "envs/frontend/staging/providers.tf"
      provides: "terraform {} block now has required_version = \"~> 1.15\"; provider block unchanged"
      contains: 'required_version = "~> 1.15"'
  key_links:
    - from: "envs/frontend/staging/backend.tf use_lockfile argument"
      to: "S3 .tflock sentinel object at s3://dmair-terraform-prod/frontend/staging/terraform.tfstate.tflock"
      via: "S3-native locking (Terraform 1.10+ feature)"
      pattern: "use_lockfile = true"
---

<objective>
Rewire the frontend staging stack (`staging.flydmair.com`) to S3-native state locking via a one-line edit to `envs/frontend/staging/backend.tf`, plus a sibling one-line edit to `envs/frontend/staging/providers.tf` pinning `required_version = "~> 1.15"`. Third and final live-stack lock-mechanism rewire per D-11.

Purpose: Completes BOOTSTRAP-02 — every existing live backend (`envs/strapi`, `envs/frontend/prod`, `envs/frontend/staging`) now uses `use_lockfile = true` and is re-initialized; `required_version = "~> 1.15"` is pinned across the three env stacks.

Output: One line added to `envs/frontend/staging/backend.tf` (`use_lockfile = true`). One line added to `envs/frontend/staging/providers.tf` (`required_version = "~> 1.15"`). Two independent atomic commits (one per file).

Hard invariant: ZERO resource managed by `envs/frontend/staging` may change. `terraform plan` after `init -reconfigure` MUST report `No changes`. Revert the offending commit if non-empty plan (D-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-bootstrap-state-backend/01-CONTEXT.md
@.planning/phases/01-bootstrap-state-backend/01-RESEARCH.md
@.planning/phases/01-bootstrap-state-backend/01-PATTERNS.md
@.planning/phases/01-bootstrap-state-backend/01-04-SUMMARY.md
@envs/frontend/staging/backend.tf
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add use_lockfile and required_version to envs/frontend/staging/{backend.tf, providers.tf}</name>
  <files>envs/frontend/staging/backend.tf, envs/frontend/staging/providers.tf</files>
  <read_first>
    - envs/frontend/staging/backend.tf (current 9-line shape; identical to envs/strapi/backend.tf except for `key`)
    - envs/frontend/staging/providers.tf (current shape; insertion target is inside the `terraform {}` block above `required_providers`)
    - envs/strapi/backend.tf + envs/strapi/providers.tf, envs/frontend/prod/backend.tf + envs/frontend/prod/providers.tf (already-rewired shapes from Plans 01-03 and 01-04 — same insertions to apply here)
    - .planning/phases/01-bootstrap-state-backend/01-PATTERNS.md (§envs/frontend/staging/backend.tf — "same 1-line insertion at same position, same alignment. Order: THIRD")
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-11 — this is the THIRD/FINAL rewire; D-13 — independently-revertable atomic commits)
  </read_first>
  <action>
    Insert exactly ONE line into `envs/frontend/staging/backend.tf` inside the `backend "s3" {}` block, immediately after the `shared_credentials_files` line and before the closing brace:
    `    use_lockfile             = true`

    Spacing: 4-space leading indent, `use_lockfile` followed by 13 spaces to align `=` at column 30. Value is `true` (bare bool).

    Then, in `envs/frontend/staging/providers.tf`, inside the existing `terraform {}` block, add a sibling argument `required_version = "~> 1.15"` as the FIRST line inside the block (above `required_providers`). Pins the Terraform CLI floor to 1.15.x.

    Do NOT change any other line in either file. Run `terraform fmt envs/frontend/staging/backend.tf envs/frontend/staging/providers.tf` after the edits. Implements D-11 (third rewire) and D-13 (TWO commits — backend.tf is one commit, providers.tf is the other).
  </action>
  <verify>
    <automated>grep -q 'use_lockfile             = true' envs/frontend/staging/backend.tf &amp;&amp; test $(wc -l &lt; envs/frontend/staging/backend.tf) -eq 10 &amp;&amp; grep -q 'required_version = "~> 1.15"' envs/frontend/staging/providers.tf &amp;&amp; terraform fmt -check envs/frontend/staging/backend.tf envs/frontend/staging/providers.tf</automated>
  </verify>
  <done>
    - `envs/frontend/staging/backend.tf` is 10 lines
    - Contains the exact line `    use_lockfile             = true` with `=` at column 30
    - `envs/frontend/staging/providers.tf` contains `required_version = "~> 1.15"` in the terraform {} block above `required_providers`
    - `terraform fmt -check` reports no diff on either file
    - `envs/strapi/` and `envs/frontend/prod/` are untouched in this plan (already rewired in Plans 01-03 / 01-04)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator runs terraform init -reconfigure + plan in envs/frontend/staging and verifies zero-change</name>
  <read_first>
    - envs/frontend/staging/backend.tf (just-edited, +1 line)
    - envs/frontend/staging/providers.tf (just-edited, +1 line)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Code Examples §Example 6; §Pitfall 5)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-12, D-13)
  </read_first>
  <what-built>Claude added one line to `envs/frontend/staging/backend.tf` (`use_lockfile = true`) and one line to `envs/frontend/staging/providers.tf` (`required_version = "~> 1.15"`). Operator runs `terraform init -reconfigure` then `terraform plan` and verifies `No changes`.</what-built>
  <how-to-verify>
    From repo root:

    1. `cd envs/frontend/staging && terraform init -reconfigure`
       - Expected: `Successfully configured the backend "s3"!`. No `-migrate-state` prompt. Answer NO if prompted (Pitfall 5) and STOP.

    2. `terraform plan`
       - **CRITICAL — BOOTSTRAP-02 final success gate.** Expected: `No changes. Your infrastructure matches the configuration.`
       - No deprecation warnings expected — use_lockfile is the current HashiCorp-recommended pattern.
       - If plan shows any diff: STOP, revert the offending commit only (D-13), do NOT commit.

    3. (Phase-wide sanity check) Run zero-change plans in ALL FOUR stacks in succession to prove the live-infra-is-sacred invariant holds across the entire phase:
       - `cd bootstrap && terraform plan` → expect `No changes.`
       - `cd envs/strapi && terraform plan` → expect `No changes.`
       - `cd envs/frontend/prod && terraform plan` → expect `No changes.`
       - `cd envs/frontend/staging && terraform plan` → expect `No changes.`

    Paste all four `No changes.` confirmations into the resume message.
  </how-to-verify>
  <acceptance_criteria>
    - `cd envs/frontend/staging && terraform init -reconfigure` exits 0 without prompting for `-migrate-state`
    - `cd envs/frontend/staging && terraform plan` exits 0 with stdout containing `No changes. Your infrastructure matches the configuration.`
    - All four stacks (`bootstrap/`, `envs/strapi`, `envs/frontend/prod`, `envs/frontend/staging`) currently report `No changes` — BOOTSTRAP-02 fully satisfied AND Phase 1 zero-change-plan invariant proven across all stacks
    - Operator commits as TWO standalone commits — `feat(BOOTSTRAP-02): enable use_lockfile on frontend-staging backend` and `chore(frontend/staging): pin required_version to ~> 1.15` (each independently revertable per D-13)
  </acceptance_criteria>
  <resume-signal>Type "frontend-staging-rewired" with all four No-changes confirmations, or describe failure + revert action.</resume-signal>
  <files>(operator-only checkpoint — no files modified by Claude)</files>
  <action>See &lt;how-to-verify&gt; above. This is an operator-only checkpoint — Claude does NOT execute commands. The orchestrator MUST present &lt;what-built&gt; and &lt;how-to-verify&gt; to the human operator and wait for the resume signal.</action>
  <verify>
    <automated>echo 'checkpoint: human verification required — see &lt;acceptance_criteria&gt;'; true</automated>
  </verify>
  <done>See &lt;acceptance_criteria&gt; above. Human operator must confirm via resume-signal.</done>
</task>

</tasks>

<verification>
- envs/frontend/staging/backend.tf has the new `use_lockfile = true` line; file is 10 lines.
- envs/frontend/staging/providers.tf has the new `required_version = "~> 1.15"` line.
- All four stacks (`bootstrap/`, `envs/strapi`, `envs/frontend/prod`, `envs/frontend/staging`) plan clean.
- BOOTSTRAP-02 fully satisfied via S3-native state locking.
- Each commit is independently revertable per D-13.
</verification>

<success_criteria>
- BOOTSTRAP-02 complete: 3 of 3 live-stack rewires done with zero-change plans verified.
- Live-infra-is-sacred invariant holds across all four stacks simultaneously.
- Phase 1 ready for BOOTSTRAP-03 verification (Plan 01-06 — manual two-terminal lock-contention test).
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-state-backend/01-05-SUMMARY.md` with: confirmation of edit, pasted four-stack zero-change-plan confirmations (one per stack), commit SHA.
</output>
