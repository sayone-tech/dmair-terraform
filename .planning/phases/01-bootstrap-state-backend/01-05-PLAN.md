---
phase: 01-bootstrap-state-backend
plan: 05
type: execute
wave: 5
depends_on:
  - "01-04"
files_modified:
  - envs/frontend/staging/backend.tf
autonomous: false
requirements:
  - BOOTSTRAP-02

must_haves:
  truths:
    - "envs/frontend/staging/backend.tf declares dynamodb_table = \"dmair-terraform-locks\" inside the backend \"s3\" {} block"
    - "envs/frontend/staging is re-initialized via terraform init -reconfigure (NOT -migrate-state) without prompting for state migration"
    - "terraform plan in envs/frontend/staging reports 'No changes. Your infrastructure matches the configuration.' — third and final of three live-stack rewires (D-11)"
    - "BOOTSTRAP-02 fully satisfied: all three live backends now route through the DynamoDB lock table"
  artifacts:
    - path: "envs/frontend/staging/backend.tf"
      provides: "Backend block with dynamodb_table line added; identical to current shape otherwise"
      contains: 'dynamodb_table           = "dmair-terraform-locks"'
  key_links:
    - from: "envs/frontend/staging/backend.tf dynamodb_table argument"
      to: "AWS DynamoDB table dmair-terraform-locks (created in Plan 01-02)"
      via: "string-literal contract (D-06)"
      pattern: 'dynamodb_table = "dmair-terraform-locks"'
---

<objective>
Rewire the frontend staging stack (`staging.flydmair.com`) to use the new DynamoDB lock table via a one-line edit. Third and final live-stack rewire per D-11.

Purpose: Completes BOOTSTRAP-02 — every existing live backend (`envs/strapi`, `envs/frontend/prod`, `envs/frontend/staging`) is now routed through `dmair-terraform-locks` and re-initialized.

Output: One line added to `envs/frontend/staging/backend.tf`. One independent atomic commit.

Hard invariant: ZERO resource managed by `envs/frontend/staging` may change. `terraform plan` after `init -reconfigure` MUST report `No changes`. Revert this single commit if non-empty plan (D-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-bootstrap-state-backend/01-CONTEXT.md
@.planning/phases/01-bootstrap-state-backend/01-RESEARCH.md
@.planning/phases/01-bootstrap-state-backend/01-PATTERNS.md
@.planning/phases/01-bootstrap-state-backend/01-04-SUMMARY.md
@envs/frontend/staging/backend.tf
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add dynamodb_table line to envs/frontend/staging/backend.tf</name>
  <files>envs/frontend/staging/backend.tf</files>
  <read_first>
    - envs/frontend/staging/backend.tf (lines 1-9 — current 9-line shape; identical to envs/strapi/backend.tf except for `key`)
    - envs/strapi/backend.tf, envs/frontend/prod/backend.tf (already-rewired 10-line shapes from Plans 01-03 and 01-04 — same insertion to apply here)
    - .planning/phases/01-bootstrap-state-backend/01-PATTERNS.md (§envs/frontend/staging/backend.tf — "same 1-line insertion at same position, same alignment. Order: THIRD")
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-06, D-11 — this is the THIRD/FINAL rewire, D-13)
  </read_first>
  <action>
    Insert exactly ONE line into `envs/frontend/staging/backend.tf` inside the `backend "s3" {}` block, immediately after the `shared_credentials_files` line and before the closing brace:
    `    dynamodb_table           = "dmair-terraform-locks"`

    Spacing: 4-space leading indent, `dynamodb_table` followed by 11 spaces to align `=` at column 30. Value is the contract string per D-06.

    Do NOT change any other line. Do NOT touch any other file in `envs/frontend/staging/`. Run `terraform fmt envs/frontend/staging/backend.tf`. Implements D-11 (third rewire).
  </action>
  <verify>
    <automated>grep -q 'dynamodb_table           = "dmair-terraform-locks"' envs/frontend/staging/backend.tf &amp;&amp; test $(wc -l &lt; envs/frontend/staging/backend.tf) -eq 10 &amp;&amp; terraform fmt -check envs/frontend/staging/backend.tf</automated>
  </verify>
  <done>
    - `envs/frontend/staging/backend.tf` is 10 lines
    - Contains the exact line `    dynamodb_table           = "dmair-terraform-locks"` with `=` at column 30
    - `terraform fmt -check` reports no diff
    - No other file in `envs/frontend/staging/` is modified
    - `envs/strapi/` and `envs/frontend/prod/` are untouched in this plan (already rewired in Plans 01-03 / 01-04)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator runs terraform init -reconfigure + plan in envs/frontend/staging and verifies zero-change</name>
  <read_first>
    - envs/frontend/staging/backend.tf (just-edited)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Code Examples §Example 6; §Pitfall 5; §Pitfall 6)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-12, D-13)
  </read_first>
  <what-built>Claude added one line to `envs/frontend/staging/backend.tf`. Operator runs `terraform init -reconfigure` then `terraform plan` and verifies `No changes`.</what-built>
  <how-to-verify>
    From repo root:

    1. `cd envs/frontend/staging && terraform init -reconfigure`
       - Expected: `Successfully configured the backend "s3"!`. No `-migrate-state` prompt. Answer NO if prompted (Pitfall 5) and STOP.

    2. `terraform plan`
       - **CRITICAL — BOOTSTRAP-02 final success gate.** Expected: `No changes. Your infrastructure matches the configuration.`
       - Deprecation warning on `dynamodb_table` is EXPECTED (Pitfall 6).
       - If plan shows any diff: STOP, revert this commit only (D-13), do NOT commit.

    3. (Phase-wide sanity check) Run zero-change plans in ALL FOUR stacks in succession to prove the live-infra-is-sacred invariant holds across the entire phase:
       - `cd bootstrap && terraform plan` → expect `No changes.`
       - `cd envs/strapi && terraform plan` → expect `No changes.`
       - `cd envs/frontend/prod && terraform plan` → expect `No changes.`
       - `cd envs/frontend/staging && terraform plan` → expect `No changes.`

    Paste all four `No changes.` confirmations into the resume message.
  </how-to-verify>
  <acceptance_criteria>
    - `cd envs/frontend/staging && terraform init -reconfigure` exits 0 without prompting for `-migrate-state`
    - `cd envs/frontend/staging && terraform plan` exits 0 with stdout containing `No changes. Your infrastructure matches the configuration.`
    - All four stacks (`bootstrap/`, `envs/strapi`, `envs/frontend/prod`, `envs/frontend/staging`) currently report `No changes` — BOOTSTRAP-02 fully satisfied AND Phase 1 zero-change-plan invariant proven across all stacks
    - Operator commits as a standalone commit titled `feat(BOOTSTRAP-02): wire frontend-staging backend to dmair-terraform-locks DynamoDB lock table` (independently revertable per D-13)
  </acceptance_criteria>
  <resume-signal>Type "frontend-staging-rewired" with all four No-changes confirmations, or describe failure + revert action.</resume-signal>
  <files>(operator-only checkpoint — no files modified by Claude)</files>
  <action>See &lt;how-to-verify&gt; above. This is an operator-only checkpoint — Claude does NOT execute commands. The orchestrator MUST present &lt;what-built&gt; and &lt;how-to-verify&gt; to the human operator and wait for the resume signal.</action>
  <verify>
    <automated>echo 'checkpoint: human verification required — see &lt;acceptance_criteria&gt;'; true</automated>
  </verify>
  <done>See &lt;acceptance_criteria&gt; above. Human operator must confirm via resume-signal.</done>
</task>

</tasks>

<verification>
- envs/frontend/staging/backend.tf has the new dynamodb_table line; file is 10 lines.
- All four stacks (`bootstrap/`, `envs/strapi`, `envs/frontend/prod`, `envs/frontend/staging`) plan clean.
- BOOTSTRAP-02 fully satisfied.
- Commit is independently revertable per D-13.
</verification>

<success_criteria>
- BOOTSTRAP-02 complete: 3 of 3 live-stack rewires done with zero-change plans verified.
- Live-infra-is-sacred invariant holds across all four stacks simultaneously.
- Phase 1 ready for BOOTSTRAP-03 verification (Plan 01-06 — manual two-terminal lock-contention test).
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-state-backend/01-05-SUMMARY.md` with: confirmation of edit, pasted four-stack zero-change-plan confirmations (one per stack), commit SHA.
</output>
