---
phase: 01-bootstrap-state-backend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md
autonomous: false
requirements:
  - BOOTSTRAP-01
user_setup:
  - service: terraform-cli
    why: "Terraform CLI >= 1.5 required for declarative import {} block (D-05, D-12); not installed on workstation per RESEARCH §Environment Availability"
    install:
      - task: "Install Terraform CLI >= 1.5"
        command: "brew install hashicorp/tap/terraform"
        verify: "terraform version reports >= 1.5.0"
  - service: aws-profile-dmair
    why: "Local profile is fly-dmair (dmair-view IAM user, read-list only); apply step requires dmair profile with s3:Get*, s3:PutBucketTagging, dynamodb:CreateTable, dynamodb:DescribeTable, dynamodb:UpdateTable, dynamodb:TagResource (per RESEARCH §Environment Availability + Pitfall 4)"
    dashboard_config:
      - task: "Add [dmair] section to ~/.aws/credentials with write-capable IAM user/role; NOT the read-only dmair-view user"
        location: "~/.aws/credentials"
      - task: "Verify credentials with: aws --profile dmair sts get-caller-identity (must return account 071297531943 with non-dmair-view user)"
        location: "local shell"

must_haves:
  truths:
    - "Terraform CLI >= 1.5 is installed and on PATH"
    - "AWS profile dmair exists locally with write permissions to s3://dmair-terraform-prod bucket-config + dynamodb:CreateTable in us-west-2"
    - "Live AWS-side state of dmair-terraform-prod bucket (versioning, encryption, public-access-block, tagging) is captured verbatim into a snapshot artifact"
  artifacts:
    - path: ".planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md"
      provides: "Raw JSON output of aws s3api get-bucket-versioning / get-bucket-encryption / get-public-access-block / get-bucket-tagging against dmair-terraform-prod — single source of truth for bootstrap/main.tf HCL values (per D-05)"
      contains: "Status, SSEAlgorithm, BlockPublicAcls/BlockPublicPolicy/IgnorePublicAcls/RestrictPublicBuckets values"
  key_links:
    - from: ".planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md"
      to: "bootstrap/main.tf (Wave 2)"
      via: "field-by-field translation table in 01-RESEARCH.md §Code Examples §Example 1"
      pattern: "AWS CLI JSON field → HCL argument value (literal)"
---

<objective>
Resolve the two blocking environment dependencies flagged in 01-RESEARCH.md §Environment Availability and capture the live AWS-side configuration of `dmair-terraform-prod` so Wave 2 can write HCL that mirrors reality field-for-field.

Purpose: Without Terraform CLI installed and the `dmair` profile arranged, no subsequent task can run. Without the live-state capture, the HCL in `bootstrap/main.tf` will be guessed from memory and the first `terraform plan` after apply will report drift — failing BOOTSTRAP-01 success criterion 1.

Output: An installed Terraform CLI, a working `dmair` AWS profile, and a snapshot file in this phase directory recording the verbatim JSON output of four `aws s3api` inspections against the live state bucket.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-state-backend/01-CONTEXT.md
@.planning/phases/01-bootstrap-state-backend/01-RESEARCH.md
@.planning/phases/01-bootstrap-state-backend/01-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Operator installs Terraform CLI and arranges dmair AWS profile</name>
  <read_first>
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Environment Availability lines 296-310; §Standard Stack lines 78-114 — installation instructions; Pitfall 4)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-02 — provider/profile invariants)
    - envs/strapi/backend.tf (current backend block referencing profile = "dmair")
  </read_first>
  <what-built>This is operator-only setup — Claude cannot install software or write to ~/.aws/credentials.</what-built>
  <how-to-verify>
    1. Run: `terraform version` — must print `Terraform v1.5.0` or higher. If absent, install via `brew install hashicorp/tap/terraform`.
    2. Run: `aws configure list-profiles` — must include `dmair` (in addition to or instead of `fly-dmair`).
    3. Run: `aws --profile dmair sts get-caller-identity` — must return account `071297531943` with an IAM user/role that is NOT `dmair-view` (the read-only user verified in RESEARCH §Sources). The user/role MUST have permissions for: `s3:GetBucketVersioning`, `s3:GetEncryptionConfiguration`, `s3:GetBucketPublicAccessBlock`, `s3:GetBucketTagging`, `s3:PutBucketTagging`, `dynamodb:CreateTable`, `dynamodb:DescribeTable`, `dynamodb:UpdateTable`, `dynamodb:TagResource`.
    4. Run: `aws --profile dmair s3api get-bucket-versioning --bucket dmair-terraform-prod --region us-west-2` — must NOT return `AccessDenied`. (May return an empty body if versioning is unset; that is acceptable — it is `AccessDenied` that fails this gate.)
  </how-to-verify>
  <acceptance_criteria>
    - `terraform version` exits 0 and reports a version string `>= v1.5.0`
    - `aws --profile dmair sts get-caller-identity` exits 0 and the `UserId` / `Arn` field does NOT match `dmair-view`
    - `aws --profile dmair s3api get-bucket-versioning --bucket dmair-terraform-prod --region us-west-2` exits 0 (no AccessDenied)
  </acceptance_criteria>
  <resume-signal>Type "preconditions-met" once all four verification commands above succeed, or describe the blocker.</resume-signal>
  <files>(operator-only checkpoint — no files modified by Claude)</files>
  <action>See &lt;how-to-verify&gt; above. This is an operator-only checkpoint — Claude does NOT execute commands. The orchestrator MUST present &lt;what-built&gt; and &lt;how-to-verify&gt; to the human operator and wait for the resume signal.</action>
  <verify>
    <automated>echo 'checkpoint: human verification required — see &lt;acceptance_criteria&gt;'; true</automated>
  </verify>
  <done>See &lt;acceptance_criteria&gt; above. Human operator must confirm via resume-signal.</done>
</task>

<task type="auto">
  <name>Task 2: Capture live AWS-side configuration of dmair-terraform-prod into LIVE-STATE-SNAPSHOT.md</name>
  <files>.planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md</files>
  <read_first>
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Code Examples §Example 1 lines 414-475 — the four required `aws s3api` commands + expected output shapes + translation table; Pitfall 2 — why this capture is the load-bearing artifact for BOOTSTRAP-01; Pitfall 8 — why tagging capture matters; Open Question 2 — operator should ALSO add `get-bucket-policy`, `get-bucket-lifecycle-configuration`, `get-bucket-logging` to inspect for surprises)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-03 — the four resources that come under IaC; D-04 — sub-resources that stay out of IaC but should still be inspected for awareness; D-05 — capture-before-HCL invariant)
  </read_first>
  <action>
    Run each of the following AWS CLI commands and paste the FULL JSON or error response verbatim into a new file at `.planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md`. Use Markdown fenced code blocks (```json or ```text) for each capture. Title the file `# Phase 1: dmair-terraform-prod Live State Snapshot` and date it. Implements D-05.

    Required captures (BOOTSTRAP-01 load-bearing — these four become HCL literals in Wave 2):
    1. `aws --profile dmair s3api get-bucket-versioning --bucket dmair-terraform-prod --region us-west-2`
    2. `aws --profile dmair s3api get-bucket-encryption --bucket dmair-terraform-prod --region us-west-2`
    3. `aws --profile dmair s3api get-public-access-block --bucket dmair-terraform-prod --region us-west-2`
    4. `aws --profile dmair s3api get-bucket-tagging --bucket dmair-terraform-prod --region us-west-2`

    Additional captures (informational — confirms drift surface area per D-04, RESEARCH Open Question 2):
    5. `aws --profile dmair s3api get-bucket-policy --bucket dmair-terraform-prod --region us-west-2`
    6. `aws --profile dmair s3api get-bucket-lifecycle-configuration --bucket dmair-terraform-prod --region us-west-2`
    7. `aws --profile dmair s3api get-bucket-logging --bucket dmair-terraform-prod --region us-west-2`

    For each: capture the command, its exit code, and stdout/stderr. If a command returns a non-zero exit code with an error like `ServerSideEncryptionConfigurationNotFoundError`, `NoSuchPublicAccessBlockConfiguration`, `NoSuchTagSet`, or `NoSuchBucketPolicy`, RECORD that fact in the snapshot — it is a valid live-state value (means the configuration is unset).

    Also confirm the lock table does NOT yet exist by running and recording:
    8. `aws --profile dmair dynamodb describe-table --table-name dmair-terraform-locks --region us-west-2`
    Expected: `ResourceNotFoundException` (greenfield — per RESEARCH §Sources).

    At the bottom of the file, add a `## HCL Translation Decisions` section that maps each of captures 1, 2, 3 to the literal value that will appear in `bootstrap/main.tf` Wave 2 (using the translation table in 01-RESEARCH.md §Code Examples §Example 1). For example, if capture 1 shows `"Status": "Enabled"`, write: `aws_s3_bucket_versioning.this.versioning_configuration.status = "Enabled"`. Implements D-03 + D-05.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md &amp;&amp; grep -q "get-bucket-versioning" .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md &amp;&amp; grep -q "get-bucket-encryption" .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md &amp;&amp; grep -q "get-public-access-block" .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md &amp;&amp; grep -q "get-bucket-tagging" .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md &amp;&amp; grep -q "HCL Translation Decisions" .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md &amp;&amp; grep -q "ResourceNotFoundException\|dmair-terraform-locks" .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md</automated>
  </verify>
  <done>
    - `.planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md` exists
    - Contains verbatim JSON (or recorded error text) for all 8 AWS CLI captures listed above
    - Contains an `## HCL Translation Decisions` section mapping captures 1/2/3 to literal HCL argument values
    - The DynamoDB capture (#8) confirms `dmair-terraform-locks` does NOT yet exist (greenfield assumption verified)
    - File is committed to git as part of this plan's final commit
  </done>
</task>

</tasks>

<verification>
- Operator confirms `terraform version` >= 1.5 and `aws --profile dmair sts get-caller-identity` returns a write-capable identity.
- `01-LIVE-STATE-SNAPSHOT.md` contains all required captures and the HCL Translation Decisions section.
- Plan 01-02 cannot start until the snapshot file exists and Task 1 is confirmed.
</verification>

<success_criteria>
- All Wave 0 blockers in 01-RESEARCH.md §Environment Availability are resolved.
- The bootstrap/main.tf HCL author in Wave 2 has a single artifact to read field-by-field instead of guessing or re-running commands. This is the single most load-bearing prerequisite for the BOOTSTRAP-01 zero-change-plan gate.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-state-backend/01-01-SUMMARY.md` documenting: which Terraform version is installed, which IAM identity the dmair profile resolves to, and a one-line per capture indicating the value that will appear in HCL (e.g., "Versioning: Enabled", "SSE: AES256, BucketKeyEnabled=false", "PAB: all-true").
</output>
