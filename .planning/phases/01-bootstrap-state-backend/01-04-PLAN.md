---
phase: 01-bootstrap-state-backend
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - envs/frontend/prod/backend.tf
  - envs/frontend/prod/providers.tf
autonomous: false
requirements:
  - BOOTSTRAP-02

must_haves:
  truths:
    - "envs/frontend/prod/backend.tf declares use_lockfile = true inside the backend \"s3\" {} block (S3-native state locking)"
    - "envs/frontend/prod/providers.tf declares required_version = \"~> 1.15\" inside the terraform {} block — use_lockfile requires Terraform >= 1.10; workstation runs 1.15.3"
    - "envs/frontend/prod is re-initialized via terraform init -reconfigure (NOT -migrate-state) without prompting for state migration"
    - "terraform plan in envs/frontend/prod reports 'No changes. Your infrastructure matches the configuration.' — second of three live-stack rewires (D-11)"
    - "The rewire is two independently-revertable commits (one per file, per D-13)"
  artifacts:
    - path: "envs/frontend/prod/backend.tf"
      provides: "Backend block with use_lockfile line added; identical to current shape otherwise"
      contains: 'use_lockfile             = true'
    - path: "envs/frontend/prod/providers.tf"
      provides: "terraform {} block now has required_version = \"~> 1.15\"; provider block unchanged"
      contains: 'required_version = "~> 1.15"'
  key_links:
    - from: "envs/frontend/prod/backend.tf use_lockfile argument"
      to: "S3 .tflock sentinel object at s3://dmair-terraform-prod/frontend/prod/terraform.tfstate.tflock"
      via: "S3-native locking (Terraform 1.10+ feature)"
      pattern: "use_lockfile = true"
    - from: "envs/frontend/prod terraform plan"
      to: "ZERO-CHANGE-PLAN gate"
      via: "live-infra-is-sacred invariant"
      pattern: "No changes. Your infrastructure matches the configuration."
---

<objective>
Rewire the frontend production stack (`www.flydmair.com`, `flydmair.com`) to S3-native state locking via a one-line edit to `envs/frontend/prod/backend.tf`, plus a sibling one-line edit to `envs/frontend/prod/providers.tf` pinning `required_version = "~> 1.15"`. Re-initialize the backend and verify a zero-change plan. Second of three live-stack lock-mechanism rewires per D-11.

Purpose: Frontend prod hosts the production marketing site (CloudFront has `prevent_destroy=true`); the rewire mechanism was proven safe on the riskier strapi stack in Plan 01-03 and is now applied to prod.

Output: One line added to `envs/frontend/prod/backend.tf` (`use_lockfile = true`). One line added to `envs/frontend/prod/providers.tf` (`required_version = "~> 1.15"`). Two independent atomic commits (one per file).

Hard invariant: ZERO resource managed by `envs/frontend/prod` may change. `terraform plan` after `init -reconfigure` MUST report `No changes`. Revert the offending commit if non-empty plan (D-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-bootstrap-state-backend/01-CONTEXT.md
@.planning/phases/01-bootstrap-state-backend/01-RESEARCH.md
@.planning/phases/01-bootstrap-state-backend/01-PATTERNS.md
@.planning/phases/01-bootstrap-state-backend/01-03-SUMMARY.md
@envs/frontend/prod/backend.tf
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add use_lockfile and required_version to envs/frontend/prod/{backend.tf, providers.tf}</name>
  <files>envs/frontend/prod/backend.tf, envs/frontend/prod/providers.tf</files>
  <read_first>
    - envs/frontend/prod/backend.tf (current 9-line shape; identical to envs/strapi/backend.tf except for the `key` value)
    - envs/frontend/prod/providers.tf (current shape; insertion target is inside the `terraform {}` block above `required_providers`)
    - envs/strapi/backend.tf + envs/strapi/providers.tf (already-rewired shapes from Plan 01-03; this plan applies the same insertions to a different env)
    - .planning/phases/01-bootstrap-state-backend/01-PATTERNS.md (§envs/frontend/prod/backend.tf — "Apply the exact same 1-line insertion at the same position, same alignment. Order: SECOND")
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Pattern 3 — use_lockfile in backend; §Pattern 4 — required_version pin)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-11 — this is the SECOND rewire; D-13 — independently-revertable atomic commits)
  </read_first>
  <action>
    Insert exactly ONE line into `envs/frontend/prod/backend.tf` inside the `backend "s3" {}` block, immediately after the `shared_credentials_files` line and before the closing brace. The new line:
    `    use_lockfile             = true`

    Spacing: 4-space leading indent, `use_lockfile` followed by 13 spaces to align `=` at column 30 (matches surrounding column-aligned `=` signs). Value is `true` (bare bool).

    Then, in `envs/frontend/prod/providers.tf`, inside the existing `terraform {}` block, add a sibling argument `required_version = "~> 1.15"` as the FIRST line inside the block (above `required_providers`). Pins the Terraform CLI floor to 1.15.x.

    Do NOT change any other line in either file. Run `terraform fmt envs/frontend/prod/backend.tf envs/frontend/prod/providers.tf` after the edits. Implements D-11 (second rewire of three) and D-13 (TWO commits — backend.tf is one commit, providers.tf is the other).
  </action>
  <verify>
    <automated>grep -q 'use_lockfile             = true' envs/frontend/prod/backend.tf &amp;&amp; test $(wc -l &lt; envs/frontend/prod/backend.tf) -eq 10 &amp;&amp; grep -q 'required_version = "~> 1.15"' envs/frontend/prod/providers.tf &amp;&amp; terraform fmt -check envs/frontend/prod/backend.tf envs/frontend/prod/providers.tf</automated>
  </verify>
  <done>
    - `envs/frontend/prod/backend.tf` is 10 lines
    - Contains the exact line `    use_lockfile             = true` with `=` at column 30
    - `envs/frontend/prod/providers.tf` contains `required_version = "~> 1.15"` in the terraform {} block above `required_providers`
    - `terraform fmt -check` reports no diff on either file
    - `envs/strapi/` and `envs/frontend/staging/` are untouched in this plan
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator runs terraform init -reconfigure + plan in envs/frontend/prod and verifies zero-change</name>
  <read_first>
    - envs/frontend/prod/backend.tf (just-edited, +1 line)
    - envs/frontend/prod/providers.tf (just-edited, +1 line)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Code Examples §Example 6 — exact sequence; §Pitfall 5 — never accept migrate-state prompt)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-12 — no migrate-state expected; D-13 — revert offending commit if non-empty plan)
  </read_first>
  <what-built>Claude added one line to `envs/frontend/prod/backend.tf` (`use_lockfile = true`) and one line to `envs/frontend/prod/providers.tf` (`required_version = "~> 1.15"`). Operator runs `terraform init -reconfigure` then `terraform plan` and verifies `No changes`.</what-built>
  <how-to-verify>
    From repo root:

    1. `cd envs/frontend/prod && terraform init -reconfigure`
       - Expected: `Successfully configured the backend "s3"!`. No prompt for `-migrate-state`. If the migrate-state prompt appears, answer NO and STOP (Pitfall 5).

    2. `terraform plan`
       - **CRITICAL — BOOTSTRAP-02 success gate (second of three).** Expected: `No changes. Your infrastructure matches the configuration.`
       - No deprecation warnings expected — use_lockfile is the current HashiCorp-recommended pattern.
       - If plan reports any managed-resource diff: STOP. Revert the offending edit (D-13). Do NOT commit.

    Paste the final `No changes.` line into the resume message.
  </how-to-verify>
  <acceptance_criteria>
    - `cd envs/frontend/prod && terraform init -reconfigure` exits 0 without prompting for `-migrate-state`
    - `cd envs/frontend/prod && terraform plan` exits 0 with stdout containing the exact string `No changes. Your infrastructure matches the configuration.`
    - Operator commits as TWO standalone commits — `feat(BOOTSTRAP-02): enable use_lockfile on frontend-prod backend` and `chore(frontend/prod): pin required_version to ~> 1.15` (each independently revertable per D-13)
    - `envs/strapi` still plans clean (sanity-check: `cd envs/strapi && terraform plan` still reports `No changes`)
  </acceptance_criteria>
  <resume-signal>Type "frontend-prod-rewired" with the No-changes line, or describe failure + revert action.</resume-signal>
  <files>(operator-only checkpoint — no files modified by Claude)</files>
  <action>See &lt;how-to-verify&gt; above. This is an operator-only checkpoint — Claude does NOT execute commands. The orchestrator MUST present &lt;what-built&gt; and &lt;how-to-verify&gt; to the human operator and wait for the resume signal.</action>
  <verify>
    <automated>echo 'checkpoint: human verification required — see &lt;acceptance_criteria&gt;'; true</automated>
  </verify>
  <done>See &lt;acceptance_criteria&gt; above. Human operator must confirm via resume-signal.</done>
</task>

</tasks>

<verification>
- envs/frontend/prod/backend.tf has the new `use_lockfile = true` line; file is 10 lines.
- envs/frontend/prod/providers.tf has the new `required_version = "~> 1.15"` line.
- `cd envs/frontend/prod && terraform plan` reports `No changes`.
- envs/strapi unaffected (still plans clean).
- envs/frontend/staging untouched (Plan 01-05).
- Each commit is independently revertable.
</verification>

<success_criteria>
- BOOTSTRAP-02 partial: 2 of 3 live-stack rewires complete.
- Live-infra-is-sacred invariant maintained across both rewired stacks.
- Plan 01-05 (`envs/frontend/staging` rewire) is now safe to start.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-state-backend/01-04-SUMMARY.md` with: confirmation of edit + alignment, pasted `terraform plan` "No changes" output, sanity-check confirmation that envs/strapi still plans clean, commit SHA.
</output>
