---
phase: 01-bootstrap-state-backend
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - envs/frontend/prod/backend.tf
autonomous: false
requirements:
  - BOOTSTRAP-02

must_haves:
  truths:
    - "envs/frontend/prod/backend.tf declares dynamodb_table = \"dmair-terraform-locks\" inside the backend \"s3\" {} block"
    - "envs/frontend/prod is re-initialized via terraform init -reconfigure (NOT -migrate-state) without prompting for state migration"
    - "terraform plan in envs/frontend/prod reports 'No changes. Your infrastructure matches the configuration.' — second of three live-stack rewires (D-11)"
    - "The rewire is a single, independently-revertable commit (D-13)"
  artifacts:
    - path: "envs/frontend/prod/backend.tf"
      provides: "Backend block with dynamodb_table line added; identical to current shape otherwise"
      contains: 'dynamodb_table           = "dmair-terraform-locks"'
  key_links:
    - from: "envs/frontend/prod/backend.tf dynamodb_table argument"
      to: "AWS DynamoDB table dmair-terraform-locks (created in Plan 01-02)"
      via: "string-literal contract (D-06)"
      pattern: 'dynamodb_table = "dmair-terraform-locks"'
    - from: "envs/frontend/prod terraform plan"
      to: "ZERO-CHANGE-PLAN gate"
      via: "live-infra-is-sacred invariant"
      pattern: "No changes. Your infrastructure matches the configuration."
---

<objective>
Rewire the frontend production stack (`www.flydmair.com`, `flydmair.com`) to use the new DynamoDB lock table via a one-line edit to `envs/frontend/prod/backend.tf`, re-initialize the backend, verify zero-change plan. Second of three live-stack rewires per D-11.

Purpose: Frontend prod hosts the production marketing site (CloudFront has `prevent_destroy=true`); the rewire mechanism was proven safe on the riskier strapi stack in Plan 01-03 and is now applied to prod.

Output: One line added to `envs/frontend/prod/backend.tf`. One independent atomic commit.

Hard invariant: ZERO resource managed by `envs/frontend/prod` may change. `terraform plan` after `init -reconfigure` MUST report `No changes`. Revert this single commit (only) if non-empty plan (D-13).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-bootstrap-state-backend/01-CONTEXT.md
@.planning/phases/01-bootstrap-state-backend/01-RESEARCH.md
@.planning/phases/01-bootstrap-state-backend/01-PATTERNS.md
@.planning/phases/01-bootstrap-state-backend/01-03-SUMMARY.md
@envs/frontend/prod/backend.tf
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add dynamodb_table line to envs/frontend/prod/backend.tf</name>
  <files>envs/frontend/prod/backend.tf</files>
  <read_first>
    - envs/frontend/prod/backend.tf (lines 1-9 — current 9-line shape; identical to envs/strapi/backend.tf except for the `key` value)
    - envs/strapi/backend.tf (lines 1-10 — already-rewired shape from Plan 01-03; this plan applies the same insertion to a different file)
    - .planning/phases/01-bootstrap-state-backend/01-PATTERNS.md (§envs/frontend/prod/backend.tf — "Apply the exact same 1-line insertion at the same position, same alignment. Order: SECOND")
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Pattern 3 — same backend block + dynamodb_table shape; §Pitfall 6)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-06, D-11 — this is the SECOND rewire, D-13)
  </read_first>
  <action>
    Insert exactly ONE line into `envs/frontend/prod/backend.tf` inside the `backend "s3" {}` block, immediately after the `shared_credentials_files` line and before the closing brace. The new line:
    `    dynamodb_table           = "dmair-terraform-locks"`

    Spacing: 4-space leading indent, `dynamodb_table` followed by 11 spaces to align `=` at column 30 (matches surrounding column-aligned `=` signs). Value is the contract string per D-06.

    Do NOT change any other line. Do NOT touch any other file in `envs/frontend/prod/`. Run `terraform fmt envs/frontend/prod/backend.tf` after the edit. Implements D-11 (second rewire of three).
  </action>
  <verify>
    <automated>grep -q 'dynamodb_table           = "dmair-terraform-locks"' envs/frontend/prod/backend.tf &amp;&amp; test $(wc -l &lt; envs/frontend/prod/backend.tf) -eq 10 &amp;&amp; terraform fmt -check envs/frontend/prod/backend.tf</automated>
  </verify>
  <done>
    - `envs/frontend/prod/backend.tf` is 10 lines
    - Contains the exact line `    dynamodb_table           = "dmair-terraform-locks"` with `=` at column 30
    - `terraform fmt -check` reports no diff
    - No other file in `envs/frontend/prod/` is modified
    - `envs/strapi/` and `envs/frontend/staging/` are untouched in this plan
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Operator runs terraform init -reconfigure + plan in envs/frontend/prod and verifies zero-change</name>
  <read_first>
    - envs/frontend/prod/backend.tf (just-edited)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Code Examples §Example 6 — exact sequence; §Pitfall 5 — never accept migrate-state prompt; §Pitfall 6 — deprecation warning is informational)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-12 — no migrate-state expected; D-13 — revert this single commit if non-empty plan)
  </read_first>
  <what-built>Claude added one line to `envs/frontend/prod/backend.tf`. Operator runs `terraform init -reconfigure` then `terraform plan` and verifies `No changes`.</what-built>
  <how-to-verify>
    From repo root:

    1. `cd envs/frontend/prod && terraform init -reconfigure`
       - Expected: `Successfully configured the backend "s3"!`. No prompt for `-migrate-state`. If the migrate-state prompt appears, answer NO and STOP (Pitfall 5).

    2. `terraform plan`
       - **CRITICAL — BOOTSTRAP-02 success gate (second of three).** Expected: `No changes. Your infrastructure matches the configuration.`
       - Deprecation warning on `dynamodb_table` is EXPECTED (Pitfall 6) and not a plan diff.
       - If plan reports any managed-resource diff: STOP. Revert the one-line edit (D-13). Do NOT commit.

    Paste the final `No changes.` line into the resume message.
  </how-to-verify>
  <acceptance_criteria>
    - `cd envs/frontend/prod && terraform init -reconfigure` exits 0 without prompting for `-migrate-state`
    - `cd envs/frontend/prod && terraform plan` exits 0 with stdout containing the exact string `No changes. Your infrastructure matches the configuration.`
    - Operator commits as a standalone commit titled `feat(BOOTSTRAP-02): wire frontend-prod backend to dmair-terraform-locks DynamoDB lock table` (or equivalent — independently revertable per D-13)
    - `envs/strapi` still plans clean (sanity-check: `cd envs/strapi && terraform plan` still reports `No changes`)
  </acceptance_criteria>
  <resume-signal>Type "frontend-prod-rewired" with the No-changes line, or describe failure + revert action.</resume-signal>
  <files>(operator-only checkpoint — no files modified by Claude)</files>
  <action>See &lt;how-to-verify&gt; above. This is an operator-only checkpoint — Claude does NOT execute commands. The orchestrator MUST present &lt;what-built&gt; and &lt;how-to-verify&gt; to the human operator and wait for the resume signal.</action>
  <verify>
    <automated>echo 'checkpoint: human verification required — see &lt;acceptance_criteria&gt;'; true</automated>
  </verify>
  <done>See &lt;acceptance_criteria&gt; above. Human operator must confirm via resume-signal.</done>
</task>

</tasks>

<verification>
- envs/frontend/prod/backend.tf has the new dynamodb_table line; file is 10 lines.
- `cd envs/frontend/prod && terraform plan` reports `No changes`.
- envs/strapi unaffected (still plans clean).
- envs/frontend/staging untouched (Plan 01-05).
- Commit is independently revertable.
</verification>

<success_criteria>
- BOOTSTRAP-02 partial: 2 of 3 live-stack rewires complete.
- Live-infra-is-sacred invariant maintained across both rewired stacks.
- Plan 01-05 (`envs/frontend/staging` rewire) is now safe to start.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-state-backend/01-04-SUMMARY.md` with: confirmation of edit + alignment, pasted `terraform plan` "No changes" output, sanity-check confirmation that envs/strapi still plans clean, commit SHA.
</output>
