---
phase: 01-bootstrap-state-backend
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - bootstrap/backend.tf
  - bootstrap/providers.tf
  - bootstrap/main.tf
  - bootstrap/.terraform.lock.hcl
autonomous: false
requirements:
  - BOOTSTRAP-01

must_haves:
  truths:
    - "bootstrap/ Terraform stack exists and is initialized against s3://dmair-terraform-prod/bootstrap/terraform.tfstate (use_lockfile = true IS set on bootstrap's own backend per revised D-01 — S3-native locking is now the chosen approach)"
    - "aws_s3_bucket.this, aws_s3_bucket_versioning.this, aws_s3_bucket_server_side_encryption_configuration.this, aws_s3_bucket_public_access_block.this are adopted into Terraform state via declarative import {} blocks — values mirror live AWS-side config verbatim (per LIVE-STATE-SNAPSHOT.md)"
    - "After successful apply, terraform plan in bootstrap/ reports 'No changes. Your infrastructure matches the configuration.' — BOOTSTRAP-01 success criterion 1"
    - "After follow-up commit removing the four import {} blocks, terraform plan in bootstrap/ STILL reports 'No changes.'"
  artifacts:
    - path: "bootstrap/backend.tf"
      provides: "Self-referential S3 backend at key=bootstrap/terraform.tfstate; use_lockfile = true (S3-native locking, D-01 revised)"
      contains: 'backend "s3" ... use_lockfile = true'
    - path: "bootstrap/providers.tf"
      provides: "hashicorp/aws pinned to 5.91.0; required_version = \"~> 1.15\"; hardcoded region=us-west-2, profile=dmair, shared_credentials_files=[~/.aws/credentials] (D-02)"
      contains: 'version = "5.91.0", required_version'
    - path: "bootstrap/main.tf"
      provides: "4 resource blocks (4 S3 sub-resources) declared inline (D-03); import {} blocks present during first apply, removed after"
      contains: "aws_s3_bucket"
    - path: "bootstrap/.terraform.lock.hcl"
      provides: "Provider lock generated by terraform init; committed for reproducibility"
      contains: "hashicorp/aws"
  key_links:
    - from: "bootstrap/main.tf aws_s3_bucket_versioning/sse/pab arguments"
      to: ".planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md §HCL Translation Decisions"
      via: "literal copy from snapshot"
      pattern: "snapshot value → HCL argument"
    - from: "bootstrap/backend.tf"
      to: "s3://dmair-terraform-prod/bootstrap/terraform.tfstate"
      via: "terraform init"
      pattern: 'key = "bootstrap/terraform.tfstate"'
---

<objective>
Create the `bootstrap/` Terraform stack, run the first apply (four IMPORTs of the existing S3 bucket sub-resources; no CREATEs), verify a zero-change plan, then remove the `import {}` blocks in a follow-up commit and re-verify zero-change. Satisfies BOOTSTRAP-01.

Purpose: The state backend becomes self-describing IaC — the existing `dmair-terraform-prod` bucket is now under Terraform management, and S3-native state locking (use_lockfile = true) is enabled on bootstrap's own backend so the three live stacks can be rewired to use the same mechanism (Plans 01-03 / 01-04 / 01-05). The follow-up `import {}` removal commit keeps the code clean per HashiCorp's documented best practice (RESEARCH §Pitfall 7).

Output: 4 new files under `bootstrap/`. No new AWS resources beyond adoption of the existing S3 bucket sub-resources.

Hard invariant: This plan introduces NO change to any resource managed by `envs/strapi`, `envs/frontend/prod`, or `envs/frontend/staging`. Those stacks are untouched in this plan (their `backend.tf` rewires happen in Plans 01-03 / 04 / 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-state-backend/01-CONTEXT.md
@.planning/phases/01-bootstrap-state-backend/01-RESEARCH.md
@.planning/phases/01-bootstrap-state-backend/01-PATTERNS.md
@.planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md
@.planning/phases/01-bootstrap-state-backend/01-01-SUMMARY.md
@envs/strapi/backend.tf
@envs/strapi/providers.tf
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create bootstrap/backend.tf and bootstrap/providers.tf from existing-stack analogs</name>
  <files>bootstrap/backend.tf, bootstrap/providers.tf</files>
  <read_first>
    - envs/strapi/backend.tf (lines 1-9 — exact shape to copy for bootstrap/backend.tf; swap only the `key` value)
    - envs/strapi/providers.tf (lines 1-14 — exact shape to copy for bootstrap/providers.tf; bootstrap variant hardcodes the provider block instead of parameterizing)
    - .planning/phases/01-bootstrap-state-backend/01-PATTERNS.md (§bootstrap/backend.tf — alignment rules; §bootstrap/providers.tf — Variant A recommendation, hardcoded values)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-01 — use_lockfile = true on bootstrap's own backend; D-02 — provider invariants)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Code Examples §Example 3 lines 558-572 — bootstrap/backend.tf full shape; §Code Examples §Example 4 lines 574-594 — bootstrap/providers.tf full shape)
  </read_first>
  <action>
    Create `bootstrap/backend.tf` by copying the `terraform { backend "s3" { ... } }` block from `envs/strapi/backend.tf:1-9` verbatim. Change only the `key` argument value from `"strapi/terraform.tfstate"` to `"bootstrap/terraform.tfstate"`. Keep the `# us-west-2` inline comment on the `region` line (CONVENTIONS HCL Formatting). Inside the `backend "s3" {}` block, AFTER the `shared_credentials_files` line and BEFORE the closing brace, add the line: `use_lockfile = true` (column-30 aligned). This enables S3-native state locking (`.tflock` sentinel object alongside `terraform.tfstate`). Replaces D-01's previous "bootstrap unlocked" with "use_lockfile = true on bootstrap" — bootstrap is no longer unlocked. Implements D-01 (revised).

    Create `bootstrap/providers.tf` using Variant A from 01-PATTERNS.md (hardcoded — RESEARCH §Recommended Project Structure). Block 1: copy `terraform { required_providers { aws = { source = "hashicorp/aws"; version = "5.91.0" } } }` from `envs/strapi/providers.tf:1-8` verbatim. In the `terraform {}` block (Block 1), add a `required_version = "~> 1.15"` argument inside the terraform block (sibling to `required_providers`). This pins the Terraform CLI floor to 1.15.x; `use_lockfile` requires ≥ 1.10 and workstation runs 1.15.3. Block 2: write a `provider "aws"` block with `region = "us-west-2"`, `shared_credentials_files = ["~/.aws/credentials"]`, `profile = "dmair"` as string literals (NOT `var.aws_*`). Two-space indentation, `=` aligned at column 30 within each block. Implements D-02.

    Do NOT create `bootstrap/variables.tf` or `bootstrap/terraform.tfvars` (per RESEARCH §Recommended Project Structure — bootstrap is one-shot, has no inputs).
  </action>
  <verify>
    <automated>test -f bootstrap/backend.tf &amp;&amp; test -f bootstrap/providers.tf &amp;&amp; grep -q 'key                      = "bootstrap/terraform.tfstate"' bootstrap/backend.tf &amp;&amp; grep -q 'use_lockfile' bootstrap/backend.tf &amp;&amp; grep -q 'version = "5.91.0"' bootstrap/providers.tf &amp;&amp; grep -q 'required_version' bootstrap/providers.tf &amp;&amp; grep -q '~> 1.15' bootstrap/providers.tf &amp;&amp; grep -q 'profile                  = "dmair"' bootstrap/providers.tf &amp;&amp; grep -q 'region                   = "us-west-2"' bootstrap/providers.tf &amp;&amp; ! test -f bootstrap/variables.tf &amp;&amp; ! test -f bootstrap/terraform.tfvars</automated>
  </verify>
  <done>
    - `bootstrap/backend.tf` exists, contains `key = "bootstrap/terraform.tfstate"` AND contains `use_lockfile = true`
    - `bootstrap/providers.tf` exists, pins `hashicorp/aws` at `5.91.0` exactly, contains `required_version = "~> 1.15"`, hardcodes region/profile/shared_credentials_files as string literals
    - No `bootstrap/variables.tf` or `bootstrap/terraform.tfvars` exists
    - Both files pass `terraform fmt -check` (no diff)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create bootstrap/main.tf with 4 resource blocks + 4 import {} blocks (S3 sub-resources only)</name>
  <files>bootstrap/main.tf</files>
  <read_first>
    - .planning/phases/01-bootstrap-state-backend/01-LIVE-STATE-SNAPSHOT.md (§HCL Translation Decisions — authoritative source for versioning/encryption/PAB literal values)
    - modules/s3/main.tf (lines 1-32 — S3 sub-resource argument shapes; copy nesting/argument names, drop count/var.* interpolation and tags)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Pattern 1 lines 186-209 — declarative import {} block; §Code Examples §Example 2 lines 479-555 — full skeleton; §Pitfall 3 — versioning Disabled is irreversible; §Pitfall 8 — tagging handling)
    - .planning/phases/01-bootstrap-state-backend/01-PATTERNS.md (§Sub-pattern 1, 3 — file composition + resource labeling)
    - .planning/phases/01-bootstrap-state-backend/01-CONTEXT.md (D-03 — every locked decision encoded in this file for the S3 sub-resources)
  </read_first>
  <action>
    Create a single `bootstrap/main.tf` file containing the following four resources and four import blocks, in this order. Two-space indentation, `=` aligned within each block, single blank line between resource blocks (CONVENTIONS HCL Formatting).

    Resource 1: `resource "aws_s3_bucket" "this"` — single argument `bucket = "dmair-terraform-prod"`. Do NOT declare `force_destroy`, `object_lock_enabled`, or `tags` (RESEARCH §Anti-Patterns + Pitfall 8). If LIVE-STATE-SNAPSHOT.md `get-bucket-tagging` capture returned actual tags (not `NoSuchTagSet`), declare `tags = { ... }` with those exact key/value pairs verbatim from the snapshot.

    Import 1: `import { to = aws_s3_bucket.this; id = "dmair-terraform-prod" }` adjacent to Resource 1.

    Resource 2: `resource "aws_s3_bucket_versioning" "this"` — `bucket = aws_s3_bucket.this.id`, nested `versioning_configuration { status = "<from snapshot>" }`. The `status` value is whichever literal the snapshot's HCL Translation Decisions section specifies (`"Enabled"`, `"Suspended"`, or `"Disabled"`). Do NOT add `mfa_delete`. Implements D-03.

    Import 2: `import { to = aws_s3_bucket_versioning.this; id = "dmair-terraform-prod" }`.

    Resource 3: `resource "aws_s3_bucket_server_side_encryption_configuration" "this"` — `bucket = aws_s3_bucket.this.id`, nested `rule { apply_server_side_encryption_by_default { sse_algorithm = "<from snapshot>" } }`. Add `bucket_key_enabled = <bool>` inside the `rule` block ONLY if the snapshot capture explicitly shows `BucketKeyEnabled: true` (otherwise omit — provider default is false). If snapshot SSEAlgorithm is `aws:kms`, also add `kms_master_key_id = "<arn from snapshot>"` inside `apply_server_side_encryption_by_default`. Implements D-03 + D-09.

    Import 3: `import { to = aws_s3_bucket_server_side_encryption_configuration.this; id = "dmair-terraform-prod" }`.

    Resource 4: `resource "aws_s3_bucket_public_access_block" "this"` — `bucket = aws_s3_bucket.this.id`, plus four bool arguments populated verbatim from the snapshot capture: `block_public_acls`, `block_public_policy`, `ignore_public_acls`, `restrict_public_buckets`. If the snapshot capture returned `NoSuchPublicAccessBlockConfiguration`, STOP and escalate to the human — this scenario isn't covered by D-03 and needs a decision (likely option: omit Resource 4 + Import 4 entirely and treat PAB as out-of-scope drift, but that's a deviation from D-03 that requires sign-off). Implements D-03.

    Import 4: `import { to = aws_s3_bucket_public_access_block.this; id = "dmair-terraform-prod" }`.

    NO other resources. NO data sources. NO locals. NO module calls. NO `modules/*` reuse (per CONTEXT Claude's Discretion + RESEARCH §Don't Hand-Roll — bootstrap declares inline only).
  </action>
  <verify>
    <automated>test -f bootstrap/main.tf &amp;&amp; grep -c 'import {' bootstrap/main.tf | grep -q '^4$' &amp;&amp; grep -q 'resource "aws_s3_bucket" "this"' bootstrap/main.tf &amp;&amp; grep -q 'resource "aws_s3_bucket_versioning" "this"' bootstrap/main.tf &amp;&amp; grep -q 'resource "aws_s3_bucket_server_side_encryption_configuration" "this"' bootstrap/main.tf &amp;&amp; grep -q 'resource "aws_s3_bucket_public_access_block" "this"' bootstrap/main.tf &amp;&amp; ! grep -q 'aws_dynamo' bootstrap/main.tf &amp;&amp; ! grep -q 'force_destroy' bootstrap/main.tf &amp;&amp; cd bootstrap &amp;&amp; terraform fmt -check</automated>
  </verify>
  <done>
    - `bootstrap/main.tf` exists with exactly 4 `resource` blocks and exactly 4 `import` blocks
    - Resource arguments mirror LIVE-STATE-SNAPSHOT.md values verbatim for versioning/encryption/PAB
    - `bootstrap/main.tf` contains NO `aws_dynamo*` resource anywhere
    - `terraform fmt -check` exits 0 with no diff
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Operator runs first apply sequence in bootstrap/ and verifies zero-change plan</name>
  <read_first>
    - bootstrap/backend.tf, bootstrap/providers.tf, bootstrap/main.tf (just-written files)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Code Examples §Example 5 lines 596-627 — exact apply sequence with expected plan/apply output; §Pitfall 2 — what a non-zero-change plan after apply means; §Pitfall 3 — versioning irreversibility)
  </read_first>
  <what-built>Claude wrote `bootstrap/{backend,providers,main}.tf`. The operator must now run `terraform init`, `terraform plan`, and `terraform apply` to adopt the four S3 sub-resources into state.</what-built>
  <how-to-verify>
    From repo root, run the following commands IN ORDER and confirm the expected outputs:

    1. `cd bootstrap && terraform init`
       - Expected: `Initializing the backend...`, `Successfully configured the backend "s3"!`, provider plugin download. Backend writes initial state to `s3://dmair-terraform-prod/bootstrap/terraform.tfstate`.

    2. `terraform plan`
       - Expected (per RESEARCH §Example 5):
         - (no CREATEs — only the 4 S3 IMPORTs)
         - `aws_s3_bucket.this` IMPORT (existing AWS resource)
         - `aws_s3_bucket_versioning.this` IMPORT
         - `aws_s3_bucket_server_side_encryption_configuration.this` IMPORT
         - `aws_s3_bucket_public_access_block.this` IMPORT
         - Final summary: `Plan: 0 to add, 4 to import, 0 to change, 0 to destroy.`
       - If `to change` is anything other than 0 for any imported resource → STOP, do NOT apply. The HCL does not match live AWS state. Re-check LIVE-STATE-SNAPSHOT.md values vs bootstrap/main.tf (Pitfall 2 — likely a versioning/SSE/PAB miss). Fix and re-plan before proceeding.

    3. `terraform apply` (type `yes` at the prompt)
       - Expected: 4 IMPORT success lines (S3 sub-resources). No CREATEs. No errors.

    4. `terraform plan` (re-run)
       - **CRITICAL — this is BOOTSTRAP-01 success criterion 1.** Expected output MUST contain: `No changes. Your infrastructure matches the configuration.`
       - If anything else appears, BOOTSTRAP-01 is failing. Do not commit. Re-check LIVE-STATE-SNAPSHOT.md and bootstrap/main.tf.

    Paste the truncated `terraform plan` output from step 4 (just the final `Plan:` / `No changes` line) into the resume message.
  </how-to-verify>
  <acceptance_criteria>
    - `cd bootstrap && terraform plan` (run AFTER terraform apply) exits 0 with stdout containing the exact string `No changes. Your infrastructure matches the configuration.`
    - `bootstrap/.terraform.lock.hcl` is created by `terraform init` and exists in the working tree
  </acceptance_criteria>
  <resume-signal>Type "bootstrap-applied" with the No-changes output from step 4, or describe the failure.</resume-signal>
  <files>(operator-only checkpoint — no files modified by Claude)</files>
  <action>See &lt;how-to-verify&gt; above. This is an operator-only checkpoint — Claude does NOT execute commands. The orchestrator MUST present &lt;what-built&gt; and &lt;how-to-verify&gt; to the human operator and wait for the resume signal.</action>
  <verify>
    <automated>echo 'checkpoint: human verification required — see &lt;acceptance_criteria&gt;'; true</automated>
  </verify>
  <done>See &lt;acceptance_criteria&gt; above. Human operator must confirm via resume-signal.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Remove the four import {} blocks from bootstrap/main.tf and re-verify zero-change plan</name>
  <files>bootstrap/main.tf</files>
  <read_first>
    - bootstrap/main.tf (current state — contains 4 import blocks)
    - .planning/phases/01-bootstrap-state-backend/01-RESEARCH.md (§Pitfall 7 lines 392-397 — HashiCorp best practice; §Code Examples §Example 5 lines 624-627 — follow-up plan must STILL be No changes)
  </read_first>
  <action>
    Delete the four `import { to = ...; id = ... }` blocks from `bootstrap/main.tf`. Leave the five `resource` blocks untouched. Run `terraform fmt` to normalize spacing. Then run `cd bootstrap && terraform plan` and confirm it STILL reports `No changes. Your infrastructure matches the configuration.` — the imports are already recorded in state from Task 3's apply, so removing the blocks is a no-op.

    If the plan suddenly reports a diff after import-block removal, STOP — something is wrong with state. Do NOT commit. Re-add the import blocks and re-investigate (likely cause: a resource argument differs from AWS-side reality and the import block was masking it via the original adoption).

    This is a separate atomic commit per RESEARCH §Pitfall 7 best practice. The commit message should be `chore(bootstrap): remove import blocks after successful adoption`.
  </action>
  <verify>
    <automated>! grep -q '^import {' bootstrap/main.tf &amp;&amp; ! grep -q '^[[:space:]]*to = aws_s3_bucket' bootstrap/main.tf &amp;&amp; cd bootstrap &amp;&amp; terraform fmt -check &amp;&amp; terraform plan -detailed-exitcode 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `bootstrap/main.tf` contains zero `import {` blocks
    - The five `resource` blocks remain unchanged
    - `terraform plan -detailed-exitcode` in `bootstrap/` exits 0 (meaning: no diff) — equivalent to "No changes" message
    - Committed as a separate commit from Task 1-3
  </done>
</task>

</tasks>

<verification>
- After Task 3: `cd bootstrap && terraform plan` reports `No changes` (BOOTSTRAP-01 ✓).
- After Task 4: same zero-change plan AND import blocks are removed.
- Existing live stacks (`envs/strapi`, `envs/frontend/prod`, `envs/frontend/staging`) are NOT touched by this plan. Their `terraform plan` outputs remain whatever they were before this plan ran (Plans 01-03/04/05 will verify zero-change as they rewire each one).
</verification>

<success_criteria>
- BOOTSTRAP-01 success criterion 1 met: `terraform plan` in `bootstrap/` reports "No changes".
- The bootstrap stack is self-describing IaC and its own backend uses S3-native state locking (use_lockfile = true).
- `bootstrap/main.tf` is clean (no leftover import blocks) and the state bucket is self-describing IaC.
</success_criteria>

<output>
After completion, create `.planning/phases/01-bootstrap-state-backend/01-02-SUMMARY.md` documenting:
- The four resources now under bootstrap/ Terraform management
- The specific literal values used for versioning/SSE/PAB (matching LIVE-STATE-SNAPSHOT.md)
- A pasted `terraform plan` "No changes" confirmation from after Task 4
- Confirmation that bootstrap/.terraform.lock.hcl was committed
</output>
